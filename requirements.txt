# Document Forgery Detection Engine — Requirements
# ──────────────────────────────────────────────────
# Kaggle already pre-installs numpy, Pillow, opencv, scikit-image,
# scikit-learn, scipy, matplotlib.
# Only pytesseract needs pip-installing on Kaggle.
#
# Local install:
#   pip install -r requirements.txt
#
# Tesseract system binary (required):
#   Kaggle / Ubuntu:  !apt-get install -y tesseract-ocr
#   Mac:              brew install tesseract
#   Windows:          https://github.com/UB-Mannheim/tesseract/wiki

# ── Core ─────────────────────────────────────────────────────────────────────
numpy>=1.24.0
Pillow>=9.0.0
opencv-python-headless>=4.7.0
scikit-image>=0.19.0
scikit-learn>=1.1.0
pytesseract>=0.3.10
scipy>=1.9.0
matplotlib>=3.6.0

# ── PDF support (optional) ───────────────────────────────────────────────────
# pypdf2>=2.0.0
# pdf2image>=1.16.0

# ── Regional language OCR (optional) ─────────────────────────────────────────
# easyocr>=1.6.0

# ── Deep learning (optional, GPU recommended) ─────────────────────────────────
# torch>=2.0.0
# torchvision>=0.15.0
# transformers>=4.30.0
